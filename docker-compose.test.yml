version: '3.8'

services:
  postgres-test:
    image: postgres:16-alpine
    container_name: midcurve-test-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: midcurve_test
    ports:
      - '5433:5432'
    volumes:
      - postgres-test-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U testuser -d midcurve_test']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-test-data:
